org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
# El JDK no se fija aquí a propósito: Gradle toma el de la variable JAVA_HOME,
# así el proyecto compila en cualquier máquina. (Antes había una ruta absoluta
# al JDK del equipo del desarrollador original, que no existe en otros.)
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

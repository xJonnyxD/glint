import 'package:flutter_test/flutter_test.dart';

// Los tests reales se agregarán módulo por módulo.
// Este archivo es un placeholder para que el proyecto compile limpio.
void main() {
  test('placeholder — setup inicial OK', () {
    expect(true, isTrue);
  });
}
